export const cart = {
    list: [],
    valPedido: 0,
    valTotal: 0,
    valTaxa: 0,
}

export const cartReducer = (state, action) => {
    switch (action.type) {
        case 'add_cart_item': {
        }
    }
}
